# La vitrine sans le champ replicas : ce gestionnaire renonce à fixer le nombre de répliques.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vitrine
spec:
  selector:
    matchLabels: {app: vitrine}
  template:
    metadata:
      labels: {app: vitrine}
    spec:
      containers:
      - name: web
        image: registry.k8s.io/e2e-test-images/agnhost:2.61
        args: [netexec, --http-port=8080]
        resources:
          requests: {cpu: 10m, memory: 16Mi}
